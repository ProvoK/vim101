let $LANG='en'
set langmenu=en

" Always show current position
set ruler

" highlight current line
set cursorline

" Basic syntax highlight
syntax enable
colorscheme torte

" Enable line numbers
set number

